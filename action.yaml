name: 'Revive Action'
description: 'Lint your Go code with Revive'
author: 'Markus Wiegand'
branding:
  icon: 'code'
  color: 'blue'
inputs:
  config:
    description: 'Path of the Revive config file'
    required: false
runs:
  using: 'docker'
  image: 'docker://morphy/revive-action'
